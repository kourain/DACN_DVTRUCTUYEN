@model IList<Footer>
<footer>
    <div class="Sc">
        <div class="a">
            <a class="ta jc xd He uf" href="#topup">
                <img loading="lazy" src="#img" class="Db wa" alt="NAME" title="NAME" />
                <img loading="lazy" src="#img" class="Db wa" alt="NAME" title="NAME" />
                <small>và nhiều hình thức thanh toán khác</small>
            </a>
        </div>
    </div>
    <div class="a">
        <div class="f"></div>
        <div class="Ad">
            <div class="ta gc nc Je">
                <div class="ta gc Je">
                    @foreach (Footer footer in Model.Where(m=>m.ParentID == 0))
                    {
                        List<Footer> listchild = Model.Where(m => m.FooterId == footer.ParentID).ToList();
                        <div class="Eb">
                            <div class="Zd _d">Giới thiệu</div>
                        @foreach(Footer childFooter in listchild)
                            {
                                <div class="ta ec Ce ia">
                                    <a href="#"> @childFooter.ItemText </a>
                                </div>
                            }
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</footer>